const codigo = " SeCrEtO123 "

const codigoSE = (codigo.trim())

const codigoMI = (codigoSE.toLowerCase())

const existe = (codigoMI.includes("secreto"))

const tamanho = (codigoMI.length)

console.log(codigoSE, codigoMI, existe, tamanho)
                                                                                         